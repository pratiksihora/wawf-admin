export enum ControlDataType {
  STRING = 'string',
  INTEGER = 'integer',
  FLOAT = 'float',
  DATE = 'date',
  DATE_TIME = 'date-time',
  DATE_RANGE = 'date-range',
  TIME = 'time',
  ARRAY = 'array',
  OBJECT = 'object',
  BOOLEAN = 'boolean'
}
