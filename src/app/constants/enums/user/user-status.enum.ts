/**
 * Default Constant enum for default features
 */
export enum UserStatus {
  ACTIVE = "Active",
  REJECTED = "Rejected",
  INVITED = "Invited"
}

/**
 * Default Constant enum for default features
 */
export enum UserStatusColor {
  Active = "success",
  Rejected = "danger",
  Invited = "warning"
}