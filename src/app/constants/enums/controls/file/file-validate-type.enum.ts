// Note:- converted to lower case and without dash due to load in html
export enum FileValidationType {
  EXTENTION = 'extention',
  MIN_SIZE = 'minsize',
  MAX_SIZE = 'maxsize',
  MIN_FILE = 'minfile',
  MAX_FILE = 'maxfile',
  DIMENTION = 'dimention',
  SERVER_ERROR = 'server',
}
