/**
 * Filter By Type enum for filter by type config
 */
 export enum TableFilterType {
  EQUAL = 'equal',
  ARRAY = 'array',
  START_DATE = 'start_date',
  END_DATE = 'end_date',
  STRING = 'string',
}