export enum ColumFilterType {
  STRING = 'string',
  NUMBER = 'number',
  FLOAT = 'float',
  SELECT = 'select',
  MULTI_SELECT = 'multiselect',
  BOOLEAN = 'boolean',
  DATE = 'date',
  DATE_OPTIONAL = 'dateoptional',
  ADDRESS = 'address'
}
